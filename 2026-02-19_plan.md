# 2026-02-19 Plan

## 현재 상태
- 프론트엔드 lint/type-check 정리 완료
- 기준 명령 통과:
  - `pnpm --filter @codeinsight/frontend type-check`
  - `pnpm --filter @codeinsight/frontend lint`

## 앞으로 할 일 (저위험 순서)
1. 시각화 스모크 테스트 (언어별 1개 레슨)
- 대상: C / JavaScript / Python / Java
- 확인: 스텝 이동, 메모리 패널 렌더링, 터미널 출력, 에러 없음

2. 비-메모리 시각화 회귀 확인
- 대상: `eventLoop`, `scope`, `thisBinding`, `prototype`, `promise`, `algorithm`, `terminal`
- 확인: 각 뷰 라우팅 정상, 빈 데이터 fallback 정상

3. Java/Python/JS 어댑터 데이터 경계 테스트 추가
- 목표: 포맷이 불완전한 step 입력에서도 안전 동작 보장
- 방식: 타입가드 경계 케이스 단위 테스트 추가

4. 시각화 훅(`useLessonVisualization`) 테스트 보강
- 목표: 상속(carry-forward) 로직과 메모리 변환 로직 회귀 방지
- 케이스: viz 데이터 없음/부분 누락/legacy 필드 조합

5. CI 게이트 고정
- `@codeinsight/frontend`에 대해 lint/type-check를 PR 필수 체크로 유지
- 필요 시 lint 규칙 상태 재검토(`no-explicit-any` 지속 enforcement)

6. 문서 동기화
- `.claude/plans` 내 lint 정리 문서 상태 갱신
- 이번 리팩토링 범위/검증 명령/회귀 테스트 포인트 기록

## 작업 원칙
- 작은 배치로 진행
- 파일 단위 변경 후 즉시 `type-check` + `lint`
- 런타임 포맷 차이는 `unknown + 타입가드`로 처리
