# DATA_SCHEMA.md - C-OSINE 데이터 스키마 확장성 규칙

**목적**: C-OSINE 프로젝트 데이터 구조 변경 시 지켜야 할 확장성 원칙

---

## 🎯 핵심 원칙

### 1. **버전별/도메인별 분리 저장** (필요한 경우)
- [예시: 사용자 데이터는 `User` 테이블, 제품 데이터는 `Product` 테이블]
- [예시: API 버전이 변경될 경우 `v1`, `v2` 등으로 스키마 분리]

### 2. **필드 추가/변경 대응**
- 데이터베이스 마이그레이션 전략 정의 (예: Flyway, Liquibase, Sequelize Migrations)
- 기존 필드 변경 시 영향도 분석 및 하위 호환성 유지 노력
- 새 필드는 NULL 허용 여부, 기본값 등을 명확히 정의

### 3. **데이터 타입 일관성**
- 데이터베이스와 애플리케이션(ORM/ODM) 간의 데이터 타입 매핑 일관성 유지
- 숫자형, 문자열, 날짜/시간 등 각 데이터에 적합한 타입 사용

---

## 📊 데이터 저장 전략 (예시)

### 관계형 데이터베이스 (PostgreSQL/MySQL 등)
- [주요 테이블 목록 및 각 테이블의 목적]
- [테이블 간 관계 (ERD 참조)]
- [인덱스 전략]

### NoSQL 데이터베이스 (MongoDB/Redis 등, 필요한 경우)
- [NoSQL 사용 목적, 예: 캐싱, 실시간 데이터]
- [주요 컬렉션/키-값 구조]

---

## 📋 확장성 체크리스트

### 코드 작성 시
- [ ] 데이터베이스 스키마 변경 시 관련 ORM/ODM 모델 업데이트
- [ ] API 스키마(예: Swagger/OpenAPI) 변경 시 문서화 및 유효성 검사 규칙 업데이트
- [ ] 하드코딩된 값 대신 설정 파일 또는 환경 변수 활용

### DB 설계 시
- [ ] `ALTER TABLE` 사용 시 주의사항 숙지 및 마이그레이션 스크립트 작성
- [ ] 인덱스 추가/삭제 시 성능 영향도 분석
- [ ] 대량 데이터 처리 시 파티셔닝(Partitioning) 또는 샤딩(Sharding) 고려

---

## 🔄 마이그레이션 계획 (예시)

### 버전 출시 시 작업
1. **데이터베이스 마이그레이션 스크립트 작성**
   ```bash
   # 예시: npx sequelize-cli db:migrate
   ```
2. **백엔드 코드 변경**
   - 새 스키마에 맞춰 ORM/ODM 모델 업데이트
   - 데이터 처리 로직 수정
3. **프론트엔드 코드 변경**
   - API 응답 스키마 변경에 맞춰 데이터 파싱 로직 수정
   - 필요한 경우 UI 업데이트

---

## 📝 참고 문서

- `C-OSINE/.claude/context/backend_arch.md`
- `C-OSINE/.claude/context/frontend_arch.md`