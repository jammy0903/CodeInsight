{
  "lessonId": "java-2-2",
  "title": "null check pattern",
  "concept": "There are several patterns for safely handling nulls.",
  "content": {
    "code": "String str = getValueMaybeNull();\n\nif (str != null && str.length() > 0) {\n    System.out.println(str);\n}\n\nObjects.requireNonNull(str, \"str must not be null\");\n\nString result = (str != null) ? str : \"default\";\n\nString value = Objects.requireNonNullElse(str, \"default\");",
    "steps": [
      {
        "title": "Receiving a value that may be null",
        "explanation": "**Situation:** When you receive a value from outside (DB, API, user input), you should always suspect that it could be `null`.\nThis `str` is like a time bomb that you never know will explode.",
        "visualizationType": "javaMemory",
        "code": "String str = getValueMaybeNull();",
        "javaMemoryState": {
          "stack": [
            {
              "name": "str",
              "value": "null",
              "type": "String"
            }
          ],
          "note": "Always consider the possibility of null from external input",
          "heap": [],
          "output": []
        }
      },
      {
        "title": "Short-circuit Evaluation",
        "explanation": "**The magic of logical operations:** The `&&` (AND) operator does not even check the right side if the left condition is `false`.\nFirst, it checks `str != null` and if it is `null`, the subsequent `str.length()` is not executed, safely avoiding `NullPointerException`.",
        "keyInsight": "&& shortcut evaluation: if the first one is false, the second one is not executed",
        "visualizationType": "javaMemory",
        "code": "if (str != null && str.length() > 0) {",
        "javaMemoryState": {
          "evaluation": "str != null -> false -> code after && is not executed",
          "note": "Order matters! Null check comes first",
          "stack": [
            {
              "name": "str",
              "value": "null",
              "type": "String"
            }
          ],
          "heap": []
        }
      },
      {
        "title": "conditional execution",
        "explanation": "This line is executed only if all conditions in the preceding `if` statement (`str != null && str.length() > 0`) are `true`.\nPrints safely with `str` guaranteed to be a valid string.",
        "visualizationType": "javaMemory",
        "code": "System.out.println(str);",
        "javaMemoryState": {
          "stack": [
            {
              "name": "str",
              "value": "\"hello\"",
              "type": "String"
            }
          ],
          "note": "This line is only reachable when str is safe",
          "evaluation": null,
          "output": [
            "hello"
          ]
        }
      },
      {
        "title": "Objects.requireNonNull (Fail-fast)",
        "explanation": "**Fail-fast strategy:** “If there is a problem, solve it clearly right now instead of worrying about it later!” If `str` is `null`, it immediately throws a `NullPointerException` with a clear message, making debugging easier.",
        "keyInsight": "Fail-fast pattern",
        "visualizationType": "javaMemory",
        "code": "Objects.requireNonNull(str, \"str must not be null\");",
        "javaMemoryState": {
          "stack": [
            {
              "name": "str",
              "value": "null",
              "type": "String"
            }
          ],
          "output": [
            "Exception: str must not be null"
          ],
          "note": "If null, fails immediately with a clear error message"
        }
      },
      {
        "title": "Provide default values ​​with ternary operator",
        "explanation": "If `str` is not `null`, `str` itself is assigned, and if `str` is `null`, the default value `\"default\"` is assigned to the `result` variable.",
        "visualizationType": "javaMemory",
        "code": "String result = (str != null) ? str : \"default\";",
        "javaMemoryState": {
          "stack": [
            {
              "name": "str",
              "value": "null",
              "type": "String"
            },
            {
              "name": "result",
              "value": "\"default\"",
              "type": "String"
            }
          ],
          "output": [],
          "note": null
        }
      },
      {
        "title": "Objects.requireNonNullElse",
        "explanation": "A more readable approach available since Java 9.\nIt has the same function as the ternary operator, but the intent is clearly revealed: \"If not null, this, otherwise that.\"",
        "keyInsight": "Java 9+: Using requireNonNullElse",
        "visualizationType": "javaMemory",
        "code": "String value = Objects.requireNonNullElse(str, \"default\");",
        "javaMemoryState": {
          "stack": [
            {
              "name": "value",
              "value": "\"default\"",
              "type": "String"
            }
          ],
          "note": "More readable code (Java 9+)"
        }
      }
    ],
    "deltaFormat": true
  },
  "quiz": {
    "question": "What's wrong with if (str.length() > 0 && str != null)?",
    "options": [
      "grammar error",
      "If str is null, NPE occurs",
      "always returns false",
      "No problem"
    ],
    "correctIndex": 1,
    "explanation": "The order is wrong.\nstr.length() is executed first, so if str is null it will result in an NPE.\nThe null check must be placed first."
  },
  "misconceptions": [
    {
      "wrong": "The order of null checks does not matter",
      "correct": "Order is life.\nThe `null` check must come first (left).",
      "why": "If you check from the right, `null.method()` will already be executed and an NPE will occur.\n`&&` reads from the left and stops immediately when `false` is encountered (shortened evaluation)."
    }
  ],
  "keyTakeaway": "The null check is always first. Utilizing the Objects utility will keep your code cleaner."
}
