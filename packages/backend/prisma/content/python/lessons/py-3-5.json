{
  "lessonId": "py-3-5",
  "title": "Dictionary Mutability",
  "concept": "딕셔너리는 수정 가능(Mutable) — 추가, 수정, 삭제 모두 같은 객체를 변경한다!",
  "content": {
    "code": "person = {\"name\": \"Alice\", \"age\": 25}\nprint(f\"id = {id(person)}\")\n\nperson[\"age\"] = 26\nperson[\"city\"] = \"Seoul\"\nprint(f\"After changes: {person}\")\nprint(f\"id = {id(person)}\")\n\nref = person\nref[\"name\"] = \"Bob\"\nprint(f\"person: {person}\")",
    "steps": [
      {
        "title": "딕셔너리 생성",
        "explanation": "`person = {\"name\": \"Alice\", \"age\": 25}`는 딕셔너리를 만듭니다.\n\n이 레슨에서는 수정해도 같은 객체인지 확인합니다.",
        "keyInsightTitle": "포인트",
        "keyInsight": "딕셔너리는 수정 가능한 자료형이다.",
        "visualizationType": "terminal",
        "code": "person = {\"name\": \"Alice\", \"age\": 25}"
      },
      {
        "title": "객체 ID 확인",
        "explanation": "`id(person)`은 이 딕셔너리의 번호를 보여줍니다.\n\n나중에 다시 보고 같은 객체인지 확인합니다.",
        "visualizationType": "terminal",
        "code": "print(f\"id = {id(person)}\")"
      },
      {
        "title": "기존 키 값 수정",
        "explanation": "`person[\"age\"] = 26`은 기존 값을 수정합니다.\n\n`age`가 26으로 바뀝니다.",
        "visualizationType": "terminal",
        "code": "person[\"age\"] = 26"
      },
      {
        "title": "새로운 키-값 추가",
        "explanation": "`person[\"city\"] = \"Seoul\"`은 새 항목을 추가합니다.\n\n이것도 같은 딕셔너리를 바로 바꾸는 동작입니다.",
        "visualizationType": "terminal",
        "code": "person[\"city\"] = \"Seoul\""
      },
      {
        "title": "변경 후 내용 확인",
        "explanation": "`print(f\"After changes: {person}\")`는 바뀐 내용을 출력합니다.\n\n결과에는 수정과 추가가 모두 반영됩니다.",
        "visualizationType": "terminal",
        "code": "print(f\"After changes: {person}\")"
      },
      {
        "title": "ID 불변 확인",
        "explanation": "다시 `id(person)`을 출력해도 같은 값이 나옵니다.\n\n즉, 같은 딕셔너리를 수정한 것입니다.",
        "keyInsightTitle": "핵심",
        "keyInsight": "Mutable은 수정해도 객체 자체는 그대로다.",
        "visualizationType": "terminal",
        "code": "print(f\"id = {id(person)}\")",
        "occurrence": 2
      },
      {
        "title": "참조(별명) 생성",
        "explanation": "`ref = person`은 복사가 아닙니다.\n\n`ref`는 같은 딕셔너리를 가리키는 다른 이름입니다.",
        "keyInsightTitle": "주의",
        "keyInsight": "`=`는 복사가 아니라 같은 객체 공유일 수 있다.",
        "visualizationType": "terminal",
        "code": "ref = person"
      },
      {
        "title": "참조를 통해 수정",
        "explanation": "`ref[\"name\"] = \"Bob\"`는 `ref`를 통해 값을 바꿉니다.\n\n하지만 실제로는 원래 딕셔너리가 바뀌는 것입니다.",
        "visualizationType": "terminal",
        "code": "ref[\"name\"] = \"Bob\""
      },
      {
        "title": "원본도 변경되었는지 확인",
        "explanation": "`print(f\"person: {person}\")`를 보면 `person`도 함께 바뀌어 있습니다.\n\n두 이름이 같은 객체를 보고 있기 때문입니다.",
        "keyInsightTitle": "결론",
        "keyInsight": "같은 객체를 공유하면 한쪽 수정이 다른 쪽에도 보인다.",
        "visualizationType": "terminal",
        "code": "print(f\"person: {person}\")"
      }
    ]
  },
  "quiz": {
    "question": "딕셔너리 d의 독립적인 복사본을 만드는 방법은?",
    "options": [
      "e = d",
      "e = d.copy()",
      "e = dict(d)",
      "B와 C 모두"
    ],
    "correctIndex": 3,
    "explanation": "`d.copy()`와 `dict(d)` 모두 얕은 복사(Shallow Copy)를 만듭니다.\n`e = d`는 그냥 별명(Alias)을 만드는 것입니다.\n중첩된 딕셔너리는 `copy.deepcopy()`를 사용하세요."
  },
  "misconceptions": [
    {
      "wrong": "`d2 = d1`은 딕셔너리를 복사한다",
      "correct": "`d2 = d1`은 별명(Alias)을 만들 뿐입니다 — 둘 다 같은 객체를 가리킵니다.",
      "why": "대입(=)은 이름표를 객체에 붙이는 것이지, 객체를 복사하지 않습니다."
    },
    {
      "wrong": "리스트를 딕셔너리 키로 쓸 수 있다",
      "correct": "딕셔너리 키는 불변(Immutable) 타입이어야 합니다 (문자열, 숫자, 튜플).",
      "why": "가변 객체는 해시값이 일정하지 않으므로 키로 사용할 수 없습니다."
    }
  ],
  "keyTakeaway": "딕셔너리는 리스트처럼 Mutable! `d[key] = value`는 제자리에서 수정합니다. 별명(Aliasing)에 주의하세요!"
}
