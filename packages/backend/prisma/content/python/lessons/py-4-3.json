{
  "lessonId": "py-4-3",
  "title": "Lambda Functions",
  "concept": "이름 없는 한 줄짜리 함수 (익명 함수)",
  "content": {
    "code": "# 일반 함수\ndef add(x, y):\n    return x + y\n\n# 람다 함수 (동일한 기능)\nadd_lambda = lambda x, y: x + y\n\nprint(add(2, 3))\nprint(add_lambda(2, 3))\n\n# 활용: 정렬 키\npairs = [(1, 'one'), (3, 'three'), (2, 'two')]\npairs.sort(key=lambda p: p[1]) # 두 번째 요소(글자)로 정렬\nprint(pairs)",
    "steps": [
      {
        "line": 6,
        "title": "lambda 문법",
        "explanation": "**`lambda 인자: 표현식`**\n`return` 키워드 없이 결과값만 딱 적습니다.\n주로 다른 함수의 인자(`key`, `callback`)로 잠깐 쓰고 버릴 때 사용합니다.",
        "highlight": [
          6
        ],
        "visualizationType": "terminal",
        "memoryState": {
          "output": []
        }
      },
      {
        "line": 13,
        "title": "실전 활용 (sort)",
        "explanation": "`key=lambda p: p[1]`\n\"리스트의 각 요소 `p`를 받아서, 정렬 기준으로 `p[1]`을 써라\"라는 뜻입니다.\n이거 없이 별도 함수(`def get_second...`)를 만들면 코드가 길어집니다.",
        "highlight": [
          13
        ],
        "visualizationType": "terminal",
        "memoryState": {
          "output": [
            "5",
            "5",
            "[(1, 'one'), (3, 'three'), (2, 'two')]"
          ]
        }
      }
    ]
  },
  "quiz": {
    "question": "람다 함수에 대한 설명으로 틀린 것은?",
    "options": [
      "이름이 없다 (익명 함수)",
      "여러 줄의 복잡한 로직을 담을 수 있다",
      "표현식(Expression) 하나만 올 수 있다",
      "변수에 할당해서 쓸 수 있다"
    ],
    "correctIndex": 1,
    "explanation": "람다는 오직 **단일 표현식**만 가능합니다. `if-else`도 한 줄(`True if cond else False`)로 써야 하며, 여러 줄이나 복잡한 문장(`for`, `while`)은 못 씁니다."
  },
  "misconceptions": [
    {
      "wrong": "람다는 일반 함수보다 빠르다",
      "correct": "속도 차이는 없습니다.",
      "why": "단지 코드를 간결하게(Syntactic Sugar) 만드는 용도일 뿐, 내부적으로는 똑같은 Function 객체입니다."
    }
  ],
  "keyTakeaway": "간단한 함수는 `lambda`로 한 줄 컷! 정렬이나 필터링에 아주 유용하다."
}