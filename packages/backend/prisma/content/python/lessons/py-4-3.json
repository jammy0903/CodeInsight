{
  "lessonId": "py-4-3",
  "title": "Lambda Functions",
  "concept": "이름 없는 한 줄짜리 함수 (익명 함수)",
  "content": {
    "code": "# 일반 함수\ndef add(x, y):\n    return x + y\n\n# 람다 함수 (동일한 기능)\nadd_lambda = lambda x, y: x + y\n\nprint(add(2, 3))\nprint(add_lambda(2, 3))\n\n# 활용: 정렬 키\npairs = [(1, 'one'), (3, 'three'), (2, 'two')]\npairs.sort(key=lambda p: p[1]) # 두 번째 요소(글자)로 정렬\nprint(pairs)",
    "steps": [
      {
        "line": 2,
        "title": "일반 함수 정의",
        "explanation": "`def add(x, y):`\n두 수를 더하는 간단한 함수 `add`를 정의합니다.",
        "highlight": [2, 3]
    },
    {
        "line": 6,
        "title": "람다(Lambda) 함수",
        "explanation": "`add_lambda = lambda x, y: x + y`\n`add` 함수와 똑같은 기능을 하는 익명 함수를 만들어 `add_lambda` 변수에 할당합니다.",
        "highlight": [6]
    },
    {
        "line": 8,
        "title": "일반 함수 호출",
        "explanation": "`print(add(2, 3))`\n`add` 함수를 호출합니다. 실행 흐름이 함수 내부로 이동합니다.",
        "highlight": [8]
    },
    {
        "line": 3,
        "title": "add 함수 실행",
        "explanation": "`return x + y`\n`x=2`, `y=3`이므로 `5`를 반환합니다. `print`가 `5`를 출력합니다.",
        "highlight": [3]
    },
    {
        "line": 9,
        "title": "람다 함수 호출",
        "explanation": "`print(add_lambda(2, 3))`\n람다 함수를 호출합니다.",
        "highlight": [9]
    },
    {
        "line": 6,
        "title": "람다 함수 실행",
        "explanation": "`lambda x, y: x + y`\n람다 함수의 본문이 실행됩니다. `x=2`, `y=3`이므로 `5`를 반환합니다.",
        "highlight": [6]
    },
    {
        "line": 12,
        "title": "정렬할 리스트 준비",
        "explanation": "`pairs = [...]`\n(숫자, 문자열) 형태의 튜플을 담은 리스트를 생성합니다.",
        "highlight": [12]
    },
    {
        "line": 13,
        "title": "람다를 활용한 정렬",
        "explanation": "`pairs.sort(key=lambda p: p[1])`\n`key` 인자로 람다 함수를 전달합니다. 정렬 과정에서 각 요소마다 람다가 호출되어 정렬 기준값을 계산합니다.",
        "highlight": [13]
    },
    {
        "line": 13,
        "title": "람다 호출 (정렬 중)",
        "explanation": "정렬 과정에서 람다 함수가 각 요소에 대해 호출됩니다. `(1, 'one')` → `'one'`, `(3, 'three')` → `'three'`, `(2, 'two')` → `'two'`",
        "highlight": [13]
    },
    {
        "line": 13,
        "title": "람다 호출 (정렬 중)",
        "explanation": "알파벳 순으로 `'one'` < `'three'` < `'two'`이므로 정렬 결과는 `[(1, 'one'), (3, 'three'), (2, 'two')]`가 됩니다.",
        "highlight": [13]
    },
    {
        "line": 13,
        "title": "람다 호출 (정렬 중)",
        "explanation": "정렬이 완료되었습니다.",
        "highlight": [13]
    },
    {
        "line": 14,
        "title": "정렬 결과 출력",
        "explanation": "`print(pairs)`\n정렬된 리스트를 출력합니다.",
        "highlight": [14]
    }
    ]
  },
  "quiz": {
    "question": "람다 함수에 대한 설명으로 틀린 것은?",
    "options": [
      "이름이 없다 (익명 함수)",
      "여러 줄의 복잡한 로직을 담을 수 있다",
      "표현식(Expression) 하나만 올 수 있다",
      "변수에 할당해서 쓸 수 있다"
    ],
    "correctIndex": 1,
    "explanation": "람다는 오직 **단일 표현식**만 가능합니다. `if-else`도 한 줄(`True if cond else False`)로 써야 하며, 여러 줄이나 복잡한 문장(`for`, `while`)은 못 씁니다."
  },
  "misconceptions": [
    {
      "wrong": "람다는 일반 함수보다 빠르다",
      "correct": "속도 차이는 없습니다.",
      "why": "단지 코드를 간결하게(Syntactic Sugar) 만드는 용도일 뿐, 내부적으로는 똑같은 Function 객체입니다."
    }
  ],
  "keyTakeaway": "간단한 함수는 `lambda`로 한 줄 컷! 정렬이나 필터링에 아주 유용하다."
}