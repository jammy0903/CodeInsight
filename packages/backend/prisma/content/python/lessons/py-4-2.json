{
  "lessonId": "py-4-2",
  "title": "Arguments & Return",
  "concept": "함수에 값을 전달하고(Argument) 결과를 받아온다(Return)",
  "content": {
    "code": "def add(a, b=10):\n    return a + b\n\n# 1. 위치 인자 (Positional)\nres1 = add(5, 3) # 5+3\n\n# 2. 키워드 인자 (Keyword)\nres2 = add(b=5, a=3) # 순서 바꿔도 OK\n\n# 3. 디폴트 인자 (Default)\nres3 = add(7) # 7+10 (b는 기본값)\n\nprint(res1, res2, res3)",
    "steps": [
      {
        "line": 1,
        "title": "매개변수와 디폴트 값",
        "explanation": "**`def add(a, b=10):`**\n`add` 함수를 정의합니다. `b=10`처럼 매개변수에 기본값을 설정할 수 있습니다.",
        "highlight": [ 1, 2 ]
      },
      {
        "line": 5,
        "title": "위치 인자로 함수 호출",
        "explanation": "**`res1 = add(5, 3)`**\n`5`는 `a`에, `3`은 `b`에 순서대로 전달됩니다. 실행 흐름이 함수 내부로 이동합니다.",
        "highlight": [ 5 ]
      },
      {
        "line": 2,
        "title": "add 함수 실행 (5+3)",
        "explanation": "**`return a + b`**\n`a=5`, `b=3`이므로 `5 + 3 = 8`을 계산하고 반환합니다. `res1`에 `8`이 저장됩니다.",
        "highlight": [ 2 ]
      },
      {
        "line": 8,
        "title": "키워드 인자로 함수 호출",
        "explanation": "**`res2 = add(b=5, a=3)`**\n이름을 지정하여 순서와 상관없이 값을 전달합니다. 실행 흐름이 함수 내부로 이동합니다.",
        "highlight": [ 8 ]
      },
      {
        "line": 2,
        "title": "add 함수 실행 (3+5)",
        "explanation": "**`return a + b`**\n`a=3`, `b=5`이므로 `3 + 5 = 8`을 계산하고 반환합니다. `res2`에 `8`이 저장됩니다.",
        "highlight": [ 2 ]
      },
      {
        "line": 11,
        "title": "디폴트 인자 사용",
        "explanation": "**`res3 = add(7)`**\n`a`에만 `7`을 전달했습니다. `b`는 기본값 `10`을 사용합니다.",
        "highlight": [ 11 ]
      },
      {
        "line": 2,
        "title": "add 함수 실행 (7+10)",
        "explanation": "**`return a + b`**\n`a=7`, `b=10`(기본값)이므로 `7 + 10 = 17`을 계산하고 반환합니다. `res3`에 `17`이 저장됩니다.",
        "highlight": [ 2 ]
      },
      {
        "line": 13,
        "title": "최종 결과 출력",
        "explanation": "**`print(res1, res2, res3)`**\n`8`, `8`, `17`을 한 번에 출력합니다.",
        "highlight": [ 13 ]
      }
    ]
  },
  "quiz": {
    "question": "def func(a, b=[]): ... 의 문제점은?",
    "options": [
      "문법 에러다",
      "b의 기본값 리스트가 모든 호출에서 공유된다 (Mutable Default Argument)",
      "리스트는 디폴트 인자로 쓸 수 없다",
      "문제 없다"
    ],
    "correctIndex": 1,
    "explanation": "리스트 같은 가변 객체(Mutable)를 디폴트로 쓰면, 함수가 실행될 때마다 그 리스트가 계속 재사용됩니다. 데이터가 누적되는 끔찍한 버그가 생깁니다! (대신 `b=None`을 쓰세요)"
  },
  "misconceptions": [
    {
      "wrong": "파이썬은 Call by Value다",
      "correct": "파이썬은 **Call by Object Reference** (또는 Call by Assignment) 입니다.",
      "why": "함수에 객체를 넘기면 이름표(참조)가 복사됩니다. 리스트를 넘겨서 내부를 `append`하면 원본도 바뀝니다!"
    }
  ],
  "keyTakeaway": "키워드 인자로 명확하게! 가변 객체(List)는 절대 디폴트 값으로 쓰지 말자."
}