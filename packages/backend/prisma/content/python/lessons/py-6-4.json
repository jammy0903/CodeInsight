{
  "lessonId": "py-6-4",
  "title": "클래스 변수 공유 함정",
  "concept": "클래스 변수는 모든 인스턴스가 공유 - 가변 객체면 대참사!",
  "content": {
    "code": "class Student:\n    grades = []  # 클래스 변수 - 함정!\n    \n    def add_grade(self, grade):\n        self.grades.append(grade)\n\ns1 = Student()\ns2 = Student()\n\ns1.add_grade(90)\ns2.add_grade(80)\n\nprint(f\"s1: {s1.grades}\")\nprint(f\"s2: {s2.grades}\")\nprint(f\"같은 객체? {s1.grades is s2.grades}\")",
    "steps": [
      {
        "line": 1,
        "title": "클래스 정의와 클래스 변수",
        "explanation": "`Student` 클래스를 정의합니다. 이때 `grades = []`는 **클래스 변수**로, 이 클래스로 만드는 모든 객체가 **하나의 리스트를 공유**하게 되는, 흔히 발생하는 버그의 원인입니다.",
        "highlight": [1, 2, 4, 5]
    },
    {
        "line": 7,
        "title": "첫 번째 학생 생성",
        "explanation": "`s1 = Student()`\n첫 번째 `Student` 객체를 생성합니다. `s1`은 클래스에 있는 공유 `grades` 리스트를 사용하게 됩니다.",
        "highlight": [7]
    },
    {
        "line": 8,
        "title": "두 번째 학생 생성",
        "explanation": "`s2 = Student()`\n두 번째 `Student` 객체를 생성합니다. `s2` 역시 `s1`과 **똑같은 공유 `grades` 리스트**를 바라보게 됩니다.",
        "highlight": [8]
    },
    {
        "line": 10,
        "title": "s1에 성적 추가",
        "explanation": "`s1.add_grade(90)`\n`s1`을 통해 공유 `grades` 리스트에 `90`을 추가합니다. 이제 공유 리스트는 `[90]`이 됩니다.",
        "highlight": [10]
    },
    {
        "line": 11,
        "title": "s2에 성적 추가",
        "explanation": "`s2.add_grade(80)`\n`s2`를 통해 **같은 공유 `grades` 리스트**에 `80`을 추가합니다. 이제 공유 리스트는 `[90, 80]`이 됩니다.",
        "highlight": [11]
    },
    {
        "line": 13,
        "title": "s1의 성적 확인",
        "explanation": "`print(f\"s1: {s1.grades}\")`\n`s1`의 성적을 출력합니다. `s2`가 추가한 `80`까지 포함된 `[90, 80]`이 출력되는 것을 볼 수 있습니다.",
        "highlight": [13]
    },
    {
        "line": 14,
        "title": "s2의 성적 확인",
        "explanation": "`print(f\"s2: {s2.grades}\")`\n`s2`의 성적을 출력해도, `s1`과 똑같은 공유 리스트 `[90, 80]`이 출력됩니다.",
        "highlight": [14]
    },
    {
        "line": 15,
        "title": "객체 동일성 확인",
        "explanation": "`print(f\"같은 객체? {s1.grades is s2.grades}\")`\n`s1.grades`와 `s2.grades`가 정말로 '완전히 같은 객체'인지 `is`로 확인합니다. 결과는 `True`이며, 이것이 문제의 원인임을 증명합니다.",
        "highlight": [15]
    }
    ]
  },
  "quiz": {
    "question": "클래스 변수로 리스트를 쓰면?",
    "options": [
      "각 인스턴스가 독립적인 리스트를 가진다",
      "모든 인스턴스가 같은 리스트를 공유한다",
      "에러가 발생한다",
      "자동으로 복사된다"
    ],
    "correctIndex": 1,
    "explanation": "클래스 변수는 클래스에 하나만 존재하고 모든 인스턴스가 공유합니다. 가변 객체(리스트, 딕셔너리)면 의도치 않은 공유가 발생!"
  },
  "misconceptions": [
    {
      "wrong": "클래스 안에 정의하면 각 인스턴스가 소유한다",
      "correct": "클래스 수준(메서드 밖) 정의는 클래스 변수로 공유됨",
      "why": "인스턴스 변수는 __init__ 안에서 self.x = 로 만들어야 합니다."
    },
    {
      "wrong": "클래스 변수는 쓰면 안 된다",
      "correct": "상수나 공유 설정에는 유용, 가변 객체만 조심",
      "why": "species = 'Dog' 같은 불변 값은 괜찮음. [], {}만 피하세요."
    }
  ],
  "keyTakeaway": "클래스 변수로 [], {} 쓰지 마세요! __init__에서 self.x = []로 인스턴스 변수로!"
}
