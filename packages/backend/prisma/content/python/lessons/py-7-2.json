{
  "lessonId": "py-7-2",
  "title": "del 키워드",
  "concept": "del은 객체가 아닌 이름(참조)을 삭제한다! 헷갈리기 쉬운 포인트!",
  "content": {
    "code": "a = [1, 2, 3]\nb = a  # 같은 객체\n\nprint(f\"b 삭제 전: {a}\")\n\ndel b  # b 참조만 삭제!\n\nprint(f\"a는 여전히: {a}\")\n\ntry:\n    print(b)  # NameError!\nexcept NameError:\n    print(\"b는 더 이상 존재하지 않음\")",
    "steps": [
      {
        "line": 1,
        "title": "객체 생성",
        "explanation": "`a = [1, 2, 3]`\n리스트 객체를 생성하고, 변수 `a`가 이 객체를 참조합니다.",
        "highlight": [1]
    },
    {
        "line": 2,
        "title": "별칭(참조) 생성",
        "explanation": "`b = a`\n변수 `b`도 `a`와 같은 객체를 참조하도록 합니다. 이제 하나의 객체를 `a`와 `b` 두 개의 이름으로 부를 수 있습니다.",
        "highlight": [2]
    },
    {
        "line": 4,
        "title": "삭제 전 상태 확인",
        "explanation": "`print(f\"b 삭제 전: {a}\")`\n`b`를 삭제하기 전의 상태를 확인하기 위해 `a`를 출력합니다.",
        "highlight": [4]
    },
    {
        "line": 6,
        "title": "참조 삭제 (del)",
        "explanation": "`del b`\n`del` 키워드는 객체가 아닌, 변수 `b`라는 '이름' 또는 '참조'를 삭제합니다. 리스트 객체의 참조 횟수가 하나 줄어들지만, 아직 `a`가 참조하고 있으므로 객체는 사라지지 않습니다.",
        "highlight": [6]
    },
    {
        "line": 8,
        "title": "객체 생존 확인",
        "explanation": "`print(f\"a는 여전히: {a}\")`\n`b`가 삭제된 후에도, `a`는 여전히 원래의 리스트 객체에 접근할 수 있음을 보여줍니다.",
        "highlight": [8]
    },
    {
        "line": 10,
        "title": "에러 발생 시도 (try)",
        "explanation": "`try:`\n`b`라는 이름은 `del`로 삭제되어 더 이상 존재하지 않습니다. 존재하지 않는 변수에 접근하면 에러가 발생하므로, `try...except` 구문으로 에러를 안전하게 처리하려고 시도합니다.",
        "highlight": [10]
    },
    {
        "line": 11,
        "title": "NameError 발생",
        "explanation": "`print(b)`\n삭제된 변수 `b`를 출력하려고 시도합니다. 이 시점에서 `NameError`가 발생하고, 프로그램은 즉시 `except` 블록으로 점프합니다.",
        "highlight": [11]
    },
    {
        "line": 12,
        "title": "에러 잡기 (except)",
        "explanation": "`except NameError:`\n`try` 블록에서 `NameError`가 발생했기 때문에, 프로그램의 흐름이 이 `except` 블록으로 넘어옵니다.",
        "highlight": [12]
    },
    {
        "line": 13,
        "title": "에러 처리 메시지 출력",
        "explanation": "`print(...)`\n`except` 블록 안의 코드가 실행되어, `b`가 더 이상 존재하지 않는다는 메시지를 출력합니다. `try...except` 덕분에 프로그램이 멈추지 않고 계속 실행됩니다.",
        "highlight": [13]
    }
    ]
  },
  "quiz": {
    "question": "del x 실행 후 같은 객체를 y도 참조했다면?",
    "options": [
      "y도 NameError",
      "y는 여전히 객체 접근 가능",
      "객체가 손상된다",
      "y는 None이 된다"
    ],
    "correctIndex": 1,
    "explanation": "del은 x라는 이름만 삭제합니다. y는 여전히 같은 객체를 가리키고 있으므로 정상적으로 접근 가능합니다!"
  },
  "misconceptions": [
    {
      "wrong": "del x는 x가 가리키는 객체를 삭제한다",
      "correct": "del x는 x라는 이름(참조)만 삭제한다",
      "why": "객체는 refcount=0일 때만 삭제됩니다. 다른 이름이 있으면 살아있어요!"
    },
    {
      "wrong": "del 후에도 변수는 None이 된다",
      "correct": "del 후 변수는 완전히 사라진다 (NameError)",
      "why": "x = None은 x가 None을 가리키게 함. del x는 x 자체를 제거!"
    }
  ],
  "keyTakeaway": "del은 이름(참조)만 삭제! 객체는 refcount=0일 때 자동 삭제!"
}
