{
  "lessonId": "py-13-1",
  "title": "문자열 메서드 (split, join, strip, replace)",
  "concept": "문자열은 불변(Immutable)! 메서드는 항상 새 문자열을 반환한다",
  "content": {
    "code": "text = \"  Hello, World!  \"\nstripped = text.strip()\nprint(stripped)\n\nwords = \"apple,banana,cherry\".split(\",\")\nprint(words)\n\njoined = \" - \".join(words)\nprint(joined)\n\nreplaced = \"Hello World\".replace(\"World\", \"Python\")\nprint(replaced)",
    "steps": [
      {
        "title": "공백이 포함된 문자열 생성",
        "explanation": "**`text = \"  Hello, World!  \"`** -- 문자열 객체 `\"  Hello, World!  \"`가 힙(heap) 메모리에 생성되고, `text`라는 이름표(label)가 이 객체를 가리킵니다.\n\nPython은 위에서 아래로 순서대로 실행됩니다. 이 문자열에는 앞뒤에 **공백 2칸씩**이 포함되어 있습니다.\n\n**비유:** 새로 산 옷(문자열)에 아직 가격표와 태그(불필요한 공백)가 달려있는 상태입니다. 옷을 입기 전에 태그를 떼어내야(strip) 하겠죠?\n\n**왜 이런 공백이 문제인가?** 실무에서 사용자 입력이나 파일에서 읽은 데이터에는 의도치 않은 공백이 자주 포함됩니다. 예를 들어 `\"  alice  \" == \"alice\"`는 `False`입니다! 공백 때문에 비교, 검색, 저장 등에서 문제가 생길 수 있습니다.\n\n**Python 내부 동작 (문자열 불변성):** Python의 `str` 객체는 **불변(Immutable)**입니다. 한번 생성된 문자열은 **절대 수정할 수 없습니다**. `text[0] = \"X\"` 같은 코드는 `TypeError`를 발생시킵니다. 모든 문자열 메서드는 **원본을 변경하지 않고 새로운 문자열 객체를 생성하여 반환**합니다.\n\n**주의:** `text`는 공백이 포함된 문자열 객체를 가리키는 이름표일 뿐입니다. 이 이름표 자체가 문자열을 \"담고\" 있는 것이 아니라, 메모리의 문자열 객체를 **참조(reference)**하고 있습니다.\n\n**실행 순서:** `str` 객체 `\"  Hello, World!  \"` 생성 → `text` 이름표 붙임 → 다음 줄로 이동",
        "visualizationType": "pythonMemory",
        "code": "text = \"  Hello, World!  \"",
        "pythonMemoryState": {
          "output": [],
          "note": "앞뒤 공백이 포함된 문자열 생성",
          "names": [
            {
              "name": "text",
              "pointsTo": "str1",
              "highlight": true
            }
          ],
          "objects": [
            {
              "id": "str1",
              "type": "str",
              "value": "\"  Hello, World!  \"",
              "pyId": "1001",
              "highlight": true
            }
          ]
        }
      },
      {
        "title": "strip()으로 양쪽 공백 제거",
        "explanation": "**`stripped = text.strip()`** -- `text`가 가리키는 문자열 객체의 `strip()` 메서드를 호출하여, 양쪽 공백이 제거된 **새로운** 문자열 객체를 반환받습니다.\n\n**strip() 메서드:** 문자열 **양쪽 끝**의 공백 문자(스페이스, 탭 `\\t`, 줄바꿈 `\\n`, 캐리지리턴 `\\r`)를 모두 제거한 **새 문자열**을 반환합니다. 문자열 중간의 공백은 건드리지 않습니다.\n\n**비유:** 책(문자열)의 앞표지와 뒷표지(양쪽 끝)에 붙은 먼지(공백)만 털어내는 것입니다. 책 내용(중간의 공백)은 그대로 유지됩니다. 그리고 원본 책은 건드리지 않고, **깨끗한 복사본(새 문자열)**을 만들어 돌려줍니다.\n\n**Python 내부 동작 (불변성의 핵심):** `text.strip()`은 원본 문자열 `\"  Hello, World!  \"`를 **절대 수정하지 않습니다**. 대신 새로운 `str` 객체 `\"Hello, World!\"`를 힙에 생성하고, 그 참조를 반환합니다. `stripped`는 이 새 객체를 가리키는 이름표이고, `text`는 여전히 공백이 포함된 원본 객체를 가리킵니다. 두 변수는 **서로 다른 객체**를 가리킵니다.\n\n**변형 메서드:**\n- `lstrip()`: 왼쪽(Left) 공백만 제거 → `\"Hello, World!  \"`\n- `rstrip()`: 오른쪽(Right) 공백만 제거 → `\"  Hello, World!\"`\n- `strip(\"x\")`: 특정 문자 제거 → `\"xxHelloxx\".strip(\"x\")` = `\"Hello\"`\n\n**주의:** 초보자가 가장 많이 하는 실수: `text.strip()` 호출만 하고 결과를 변수에 저장하지 않는 것! `text.strip()`만 쓰면 새 문자열이 만들어졌다가 바로 사라집니다. 반드시 `stripped = text.strip()`처럼 결과를 받아야 합니다.\n\n**실행 순서:** `text.strip()` 호출 → 새 `str` 객체 `\"Hello, World!\"` 생성 → `stripped` 이름표 붙임 → `text`는 원본 그대로",
        "visualizationType": "pythonMemory",
        "code": "stripped = text.strip()",
        "pythonMemoryState": {
          "output": [],
          "note": "strip()은 새 문자열 반환 (원본 변경 없음)",
          "names": [
            {
              "name": "text",
              "pointsTo": "str1"
            },
            {
              "name": "stripped",
              "pointsTo": "str2",
              "highlight": true
            }
          ],
          "objects": [
            {
              "id": "str1",
              "type": "str",
              "value": "\"  Hello, World!  \"",
              "pyId": "1001"
            },
            {
              "id": "str2",
              "type": "str",
              "value": "\"Hello, World!\"",
              "pyId": "1002",
              "highlight": true
            }
          ]
        }
      },
      {
        "title": "strip 결과 출력",
        "explanation": "**`print(stripped)`** -- `stripped`가 가리키는 문자열 객체의 값 `\"Hello, World!\"`를 출력합니다.\n\n양쪽의 불필요한 공백이 깔끔하게 제거된 것을 확인할 수 있습니다. `text` 원본은 여전히 `\"  Hello, World!  \"` (공백 포함)입니다.\n\n**비유:** 세탁소(strip)에서 옷(문자열)을 맡겼더니, 원본은 보관하고 깨끗한 복사본을 돌려준 것입니다. 원본 옷(text)은 여전히 더러운(공백 포함) 상태입니다.\n\n**Python 내부 동작:** `print()` 함수는 내부적으로 `stripped.__str__()`을 호출하여 문자열 표현을 얻고, `sys.stdout.write()`를 통해 표준 출력에 씁니다. `str` 객체의 `__str__`은 자기 자신을 반환합니다.\n\n**실무 필수 패턴:** 사용자 입력 처리 시 `strip()`은 거의 필수입니다:\n```python\nusername = input(\"이름: \").strip()  # 입력 직후 공백 제거\nemail = input(\"이메일: \").strip().lower()  # 공백 제거 + 소문자 변환\n```\n이 패턴을 쓰지 않으면 `\" alice \"` != `\"alice\"` 때문에 로그인 실패 같은 버그가 생길 수 있습니다.\n\n**실행 순서:** `print(stripped)` → `\"Hello, World!\"` 출력 → 다음 줄로 이동",
        "visualizationType": "pythonMemory",
        "code": "print(stripped)",
        "pythonMemoryState": {
          "output": [
            "Hello, World!"
          ],
          "note": "양쪽 공백 제거 완료"
        }
      },
      {
        "title": "split()으로 문자열 분리 → 리스트",
        "explanation": "**`words = \"apple,banana,cherry\".split(\",\")`** -- 문자열을 구분자(delimiter) `\",\"`를 기준으로 나누어 **리스트(list) 객체**를 반환합니다.\n\n**split() 메서드:** 문자열을 지정한 구분자로 분리하여, 분리된 부분 문자열들의 **리스트**를 만들어 반환합니다. 원본 문자열은 변경되지 않습니다.\n\n**동작 과정을 따라가 봅시다:**\n1. `\"apple,banana,cherry\"` 문자열에서 `\",\"` (쉼표)를 찾음\n2. 첫 번째 쉼표 → `\"apple\"` 분리\n3. 두 번째 쉼표 → `\"banana\"` 분리\n4. 나머지 → `\"cherry\"` 분리\n5. 결과: `[\"apple\", \"banana\", \"cherry\"]` 리스트 반환\n\n**비유:** 긴 소시지 링크(문자열)를 가위(split)로 연결 부위(구분자)에서 잘라 개별 소시지(리스트의 각 요소)로 만드는 것입니다.\n\n**Python 내부 동작:** `split()`은 C 레벨에서 문자열을 순회하며 구분자 위치를 찾습니다. 각 구분자 사이의 부분 문자열을 새로운 `str` 객체로 생성하고, 이들의 참조를 담은 `list` 객체를 반환합니다. 구분자 자체는 결과에 포함되지 않습니다.\n\n**split() 변형:**\n- `split()` (인자 없음): 공백 기준 분리 + **연속 공백 자동 처리** → `\"a  b  c\".split()` = `[\"a\", \"b\", \"c\"]`\n- `split(\",\", 1)`: 최대 1번만 분리 → `\"a,b,c\".split(\",\", 1)` = `[\"a\", \"b,c\"]`\n\n**주의:** `\",,\".split(\",\")`는 `[\"\", \"\", \"\"]`를 반환합니다 (빈 문자열 3개). 구분자 사이에 내용이 없으면 빈 문자열이 포함됩니다!\n\n**실행 순서:** `\"apple,banana,cherry\".split(\",\")` 호출 → 쉼표 위치 탐색 → 3개 문자열 분리 → `list` 객체 생성 → `words` 이름표 붙임",
        "visualizationType": "pythonMemory",
        "code": "words = \"apple,banana,cherry\".split(\",\")",
        "pythonMemoryState": {
          "output": [],
          "note": "split(\",\")으로 쉼표 기준 분리 → 리스트",
          "names": [
            {
              "name": "text",
              "pointsTo": "str1"
            },
            {
              "name": "stripped",
              "pointsTo": "str2"
            },
            {
              "name": "words",
              "pointsTo": "list1",
              "highlight": true
            }
          ],
          "objects": [
            {
              "id": "str1",
              "type": "str",
              "value": "\"  Hello, World!  \"",
              "pyId": "1001"
            },
            {
              "id": "str2",
              "type": "str",
              "value": "\"Hello, World!\"",
              "pyId": "1002"
            },
            {
              "id": "list1",
              "type": "list",
              "value": "[\"apple\", \"banana\", \"cherry\"]",
              "pyId": "1003",
              "highlight": true
            }
          ]
        }
      },
      {
        "title": "join()으로 리스트 → 문자열 합치기",
        "explanation": "**`joined = \" - \".join(words)`** -- 리스트의 각 요소를 **구분자 문자열** `\" - \"`로 연결하여 하나의 문자열로 합칩니다.\n\n**join() 메서드:** 반복 가능한 객체(리스트, 튜플 등)의 각 요소 사이에 구분자를 삽입하여 하나의 문자열로 결합합니다.\n\n**주의 (문법이 직관적이지 않습니다!):**\n- `\" - \".join(words)` (O) -- **구분자**.join(**리스트**) 형태입니다!\n- `words.join(\" - \")` (X) -- 이렇게 쓰면 `AttributeError`! 리스트에는 `join` 메서드가 없습니다\n\n**비유:** 구슬(리스트의 각 요소)을 실(구분자)로 꿰어 목걸이(하나의 문자열)를 만드는 것입니다. \"어떤 실로 꿸 것인가\"가 먼저 오고(구분자.join), \"어떤 구슬을 꿸 것인가\"가 인자로 옵니다(리스트).\n\n**동작 과정:**\n1. `words` = `[\"apple\", \"banana\", \"cherry\"]`\n2. 첫 번째 요소: `\"apple\"`\n3. 구분자 삽입: `\" - \"`\n4. 두 번째 요소: `\"banana\"`\n5. 구분자 삽입: `\" - \"`\n6. 세 번째 요소: `\"cherry\"`\n7. 결과: `\"apple - banana - cherry\"`\n\n**Python 내부 동작:** `join()`은 먼저 모든 요소의 총 길이를 계산하여 필요한 메모리를 한 번에 할당합니다. 이후 각 요소와 구분자를 순서대로 복사합니다. 이 방식은 `+` 연산자로 문자열을 반복 연결하는 것보다 **훨씬 효율적**입니다 (`+`는 매번 새 객체를 생성하므로).\n\n**split()과 join()은 역연산 관계:** `\",\".join(\"a,b,c\".split(\",\"))` = `\"a,b,c\"` (원본 복원!)\n\n**실행 순서:** `\" - \".join(words)` 호출 → 요소 사이에 `\" - \"` 삽입 → 새 `str` 객체 생성 → `joined` 이름표 붙임",
        "visualizationType": "pythonMemory",
        "code": "joined = \" - \".join(words)",
        "pythonMemoryState": {
          "output": [],
          "note": "join()은 구분자.join(리스트) 형태",
          "names": [
            {
              "name": "text",
              "pointsTo": "str1"
            },
            {
              "name": "stripped",
              "pointsTo": "str2"
            },
            {
              "name": "words",
              "pointsTo": "list1"
            },
            {
              "name": "joined",
              "pointsTo": "str3",
              "highlight": true
            }
          ],
          "objects": [
            {
              "id": "str1",
              "type": "str",
              "value": "\"  Hello, World!  \"",
              "pyId": "1001"
            },
            {
              "id": "str2",
              "type": "str",
              "value": "\"Hello, World!\"",
              "pyId": "1002"
            },
            {
              "id": "list1",
              "type": "list",
              "value": "[\"apple\", \"banana\", \"cherry\"]",
              "pyId": "1003"
            },
            {
              "id": "str3",
              "type": "str",
              "value": "\"apple - banana - cherry\"",
              "pyId": "1004",
              "highlight": true
            }
          ]
        }
      },
      {
        "title": "replace()로 문자열 치환",
        "explanation": "**`replaced = \"Hello World\".replace(\"World\", \"Python\")`** -- 문자열에서 `\"World\"`를 찾아 `\"Python\"`으로 교체한 **새 문자열**을 반환합니다.\n\n**replace(old, new) 메서드:** 문자열에서 `old` 부분을 모두 찾아 `new`로 교체한 새 문자열을 반환합니다. 원본은 변경되지 않습니다 (불변성!).\n\n**동작 과정:**\n1. `\"Hello World\"`에서 `\"World\"` 위치를 찾습니다 (인덱스 6~10)\n2. 해당 부분을 `\"Python\"`으로 교체합니다\n3. 새 문자열 `\"Hello Python\"` 반환\n\n**비유:** 문서(문자열)의 \"찾아 바꾸기\" 기능과 같습니다. 원본 문서는 수정하지 않고, 수정된 복사본을 새로 만들어 돌려줍니다. 워드프로세서에서 Ctrl+H(찾아 바꾸기)를 누르되, 원본은 보존하는 것이죠.\n\n**Python 내부 동작:** `replace()`는 Boyer-Moore 알고리즘의 변형을 사용하여 패턴을 효율적으로 검색합니다. 찾은 위치를 기준으로 새 문자열을 조립하여 새로운 `str` 객체를 반환합니다. 원본 문자열 `\"Hello World\"`는 리터럴이므로, 이 코드 실행 후에는 어떤 이름표도 가리키지 않아 가비지 컬렉터에 의해 회수될 수 있습니다.\n\n**replace(old, new, count):** 세 번째 인자로 최대 교체 횟수를 지정할 수 있습니다:\n```python\n\"aaa\".replace(\"a\", \"b\", 2)  # \"bba\" (2번만 교체)\n\"aaa\".replace(\"a\", \"b\")     # \"bbb\" (모두 교체)\n```\n\n**주의:** `replace()`는 대소문자를 구분합니다! `\"Hello\".replace(\"hello\", \"Hi\")`는 아무것도 교체하지 않고 원본 그대로 반환합니다. 대소문자 무시 교체가 필요하면 정규식(`re.sub`)을 사용해야 합니다.\n\n**실행 순서:** `\"Hello World\".replace(\"World\", \"Python\")` 호출 → `\"World\"` 위치 탐색 → 새 `str` 객체 `\"Hello Python\"` 생성 → `replaced` 이름표 붙임",
        "visualizationType": "pythonMemory",
        "code": "replaced = \"Hello World\".replace(\"World\", \"Python\")",
        "pythonMemoryState": {
          "output": [
            "apple - banana - cherry"
          ],
          "note": "replace()로 부분 문자열 교체",
          "names": [
            {
              "name": "text",
              "pointsTo": "str1"
            },
            {
              "name": "stripped",
              "pointsTo": "str2"
            },
            {
              "name": "words",
              "pointsTo": "list1"
            },
            {
              "name": "joined",
              "pointsTo": "str3"
            },
            {
              "name": "replaced",
              "pointsTo": "str4",
              "highlight": true
            }
          ],
          "objects": [
            {
              "id": "str1",
              "type": "str",
              "value": "\"  Hello, World!  \"",
              "pyId": "1001"
            },
            {
              "id": "str2",
              "type": "str",
              "value": "\"Hello, World!\"",
              "pyId": "1002"
            },
            {
              "id": "list1",
              "type": "list",
              "value": "[\"apple\", \"banana\", \"cherry\"]",
              "pyId": "1003"
            },
            {
              "id": "str3",
              "type": "str",
              "value": "\"apple - banana - cherry\"",
              "pyId": "1004"
            },
            {
              "id": "str4",
              "type": "str",
              "value": "\"Hello Python\"",
              "pyId": "1005",
              "highlight": true
            }
          ]
        }
      }
    ],
    "deltaFormat": true
  },
  "quiz": {
    "question": "\"hello,world\".split(\",\")의 결과는?",
    "options": [
      "\"hello world\"",
      "[\"hello\", \"world\"]",
      "(\"hello\", \"world\")",
      "[\"hello,world\"]"
    ],
    "correctIndex": 1,
    "explanation": "split()은 구분자를 기준으로 문자열을 나누어 **리스트**로 반환합니다.\n쉼표를 기준으로 나누면 [\"hello\", \"world\"] 리스트가 됩니다."
  },
  "misconceptions": [
    {
      "wrong": "strip()이 원본 문자열을 수정한다",
      "correct": "문자열은 불변(Immutable)이므로 strip()은 새 문자열을 반환합니다.",
      "why": "text.strip()을 호출해도 text 변수의 값은 변하지 않습니다.\n결과를 사용하려면 stripped = text.strip()처럼 새 변수에 저장해야 합니다."
    }
  ],
  "keyTakeaway": "strip/split/join/replace는 문자열 처리 4대 메서드! 모두 새 문자열(또는 리스트)을 반환한다."
}