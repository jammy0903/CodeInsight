{
    "lessonId": "js-7-2",
    "title": "V8 메모리 구조 (Generation)",
    "concept": "V8은 메모리 효율을 위해 객체를 'Young Generation(신규)'과 'Old Generation(장기 생존)'으로 나누어 관리합니다.",
    "content": {
        "code": "// 1. Young Generation (단명 객체)\nfunction createTemp() {\n  const temp = { id: Math.random() };\n  // 함수 종료 시 참조가 끊기며 GC 대상이 됨\n}\n\n// 2. Old Generation (장수 객체)\nconst cache = [];\nfunction save(data) {\n  cache.push(data); // 전역 배열이 참조를 유지함\n}\n\ncreateTemp(); // Young 영역에 할당되었다가 금방 수거됨\ncreateTemp();\n\nsave({ type: 'important' }); // GC를 여러 번 버티면 Old 영역으로 승격됨",
        "steps": [
            {
                "line": 13,
                "title": "객체 생성 (Young Generation)",
                "explanation": "`createTemp()`가 호출되면, 내부의 `temp` 객체는 V8 힙의 **Young Generation** 영역에 할당됩니다. 대부분의 객체는 이곳에서 짧은 생을 삽니다.",
                "keyInsight": "Young Generation은 '신입' 객체들이 머무는 곳으로, GC가 매우 빠릅니다.",
                "highlight": [13, 2, 3],
                "visualizationType": "memory",
                "memoryState": { "heap": [{"area": "Young", "content": "{id: ...}"}] }
            },
            {
                "line": 13,
                "title": "마이너 GC (Scavenge)",
                "explanation": "`createTemp()` 함수 실행이 끝나면, `temp` 객체는 더 이상 참조되지 않는 '쓰레기'가 됩니다. Young Generation을 청소하는 **마이너 GC(Scavenge)**가 매우 빠르게 이 객체를 수거해갑니다.",
                "highlight": [13, 4],
                "visualizationType": "memory",
                "memoryState": { "heap": [{"area": "Young", "content": "empty (collected)"}] }
            },
            {
                "line": 14,
                "title": "반복적인 생성과 소멸",
                "explanation": "다시 `createTemp()`가 호출됩니다. 이전과 마찬가지로 `temp` 객체는 Young Generation에 생성되었다가 함수 종료와 함께 빠르게 사라집니다.",
                "highlight": [14],
                "visualizationType": "memory",
                "memoryState": { "heap": [{"area": "Young", "note": "Object created and collected again"}]}
            },
            {
                "line": 16,
                "title": "장기 생존 객체 생성",
                "explanation": "`save` 함수가 호출되면서 `{ type: 'important' }` 객체가 생성되어 `cache` 배열에 추가됩니다.",
                "highlight": [16],
                "visualizationType": "memory",
                "memoryState": { "heap": [{"area": "Young", "content": "{type: 'important'}"}] }
            },
            {
                "line": 10,
                "title": "참조 유지 (Promotion 대상)",
                "explanation": "`cache` 배열은 전역 변수이므로 프로그램이 끝날 때까지 사라지지 않습니다. 따라서 `cache`가 참조하는 객체는 마이너 GC에서 계속 살아남습니다. 몇 번의 GC에서 살아남은 '장수 객체'는 결국 **Old Generation** 영역으로 '승격(Promotion)'됩니다.",
                "keyInsight": "오래 살아남은 객체는 '안전한 보관소(Old Space)'로 옮겨서 관리 효율을 높입니다.",
                "highlight": [10],
                "visualizationType": "memory",
                "memoryState": {
                    "heap": [
                        {"area": "Young"},
                        {"area": "Old", "content": "{type: 'important'}", "note": "Promoted"}
                    ]
                }
            }
        ]
    },
    "quiz": {
        "question": "V8의 가비지 컬렉션(GC) 전략에 대한 설명으로 옳은 것은?",
        "options": [
            "모든 객체는 처음부터 Old Generation에 할당된다",
            "Young Generation은 크기가 크고 GC가 느리다",
            "대부분의 객체는 Young Generation에서 생성되어 금방 수거된다",
            "GC는 개발자가 직접 코드로 호출해야 한다"
        ],
        "correctIndex": 2,
        "explanation": "대부분의 객체는 생성 후 금방 쓸모가 없어지기 때문에(함수 종료 등), 작은 Young Generation에서 빠르게 생성하고 빠르게 수거하는 것이 효율적입니다."
    },
    "misconceptions": [
        {
            "wrong": "GC가 실행되는 동안 프로그램은 멈추지 않는다",
            "correct": "멈춥니다(Stop-the-world). 하지만 사용자가 모를 정도로 짧습니다.",
            "why": "메모리를 정리하는 동안 프로그램이 메모리를 건드리면 안 되기 때문에 잠깐 멈춰야 합니다. V8은 이 시간을 1ms 단위로 쪼개서 실행(Incremental Marking)하므로 끊김이 거의 없습니다."
        }
    ],
    "keyTakeaway": "객체 수명에 따라 메모리 영역이 나뉩니다. 불필요한 전역 변수를 줄여 메모리 누수를 방지하세요."
}